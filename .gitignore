# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — API keys must never reach the repo.
# Note: the home directory is itself a git repo on this machine, so a leaked
# .env here would be committed to a repo covering the entire user account.
.env
.env*.local
.env.local
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# test/dev artifacts
/coverage
/playwright-report
/test-results
shot*.mjs
.env*
